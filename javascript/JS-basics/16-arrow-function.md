# arrow function (66)
```js
const print = () => 10; // or
const print = _ => 10;
// if your function consists of one statement only you can ignore the "return" key word

const print = num => num;
const print = (n1, n2) => n1 + n2;

// if you have only one parameter the () are not neccessary
```
