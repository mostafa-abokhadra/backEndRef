### if condition
```js
if (condition) {
    ...
} else if {
    ...
} else {
    ...
}
```

### Ternary operator
```js
let gender = 'm';
res = gender === 'm' ? 'male' : 'female';
 
age < 20
    ? dosomething
    : age > 20 && age < 60
    ? dosomething
    : age > 60
    ? dosomething
    : dosomethingIFallFalse
```