### assignment operator (21-)

```js
let a = 20;
a = a + 20;
a += 40;
a -= 20;
```

### practice
```js
let a = 10; // 11, 12, 13
let b = "20";
let c = 80 // 81, 82

// try to find the correct output
console.log(++a + +b++ + +c++ - +a++) 11 + 21 + 80 - 12 //100
console.log(++a + -b + +c++ - -a++ + +a) 
console.log(--c + +b + --a * +b++ - +b * a + --a - +true) 

let d = "100"
let e = "20"
let f = 30
let g = true

// use d, e, f, and g variables to produce these outputs
// constraint: don't use any variable twice
// done't use any hard coded numbers
console.log('your code') // output = 2000
console.log('your code') // 173
```