### system module
```python
import sys
"""
sys.path return all of the paths that python search for you
modules in, e:g lib folder in which all of the builtin functions
and the current directory, so you can put all of your module in
certain path and append that path to sys.path and python will
import it successfully
"""
sys.path.append("path")
```